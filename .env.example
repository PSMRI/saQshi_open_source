APP_ENV=local
APP_DEBUG=false

DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=saqshi
DB_USERNAME=saqshi_user
DB_PASSWORD=change_me
DB_CONNECT_TIMEOUT=5

# Optional on Windows PHP if RSA key generation needs an OpenSSL config file.
OPENSSL_CONF=C:/php/extras/ssl/openssl.cnf
